<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ItsChristmas">
        <activity
            android:name="com.itschristmas.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ItsChristmas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="itschristmas" android:host="main" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.itschristmas.card.cardeditor.CardEditorActivity"
            android:exported="true"
            android:process=":unity_process"
            android:theme="@style/Theme.ItsChristmas" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="itschristmas" android:host="card" android:pathPrefix="/editor" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.itschristmas.card.cardshare.CardShareActivity"
            android:exported="false" >
        </activity>
    </application>

</manifest>